{
  "name": "session-artist",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:dev": "pnpm --parallel start:dev",
    "start:api": "pnpm --filter api start:dev",
    "start:web": "pnpm --filter web start:dev",
    "build": "pnpm --parallel build",
    "build:shared": "pnpm --filter shared build",
    "format": "pnpm --parallel format",
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@session-artist/shared": "workspace:*"
  }
}
